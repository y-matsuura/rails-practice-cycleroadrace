<div class="d-flex align-items-center mt-4 mb-4">
  <div class="ml-auto teams__linkBox">
    <%= link_to '一覧', teams_path, class: 'btn btn-outline-dark' %>
    <%= link_to '編集', edit_team_path(@team), class: 'btn btn-outline-dark' %>
  </div>
</div>

<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>

<%= render @team %>
