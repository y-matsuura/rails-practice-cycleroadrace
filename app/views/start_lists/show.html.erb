<div class="d-flex align-items-center mt-4 mb-4">
  <div class="ml-auto startlists__linkBox">
    <%= link_to '一覧', start_lists_path, class: 'btn btn-outline-dark' %>
    <%= link_to '編集', edit_start_list_path(@start_list), class: 'btn btn-outline-dark' %>
  </div>
</div>

<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>

<%= render @start_list %>
