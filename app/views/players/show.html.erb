<div class="d-flex align-items-center mt-4 mb-4">
  <div class="ml-auto players__linkBox">
    <%= link_to '一覧', players_path, class: 'btn btn-outline-dark' %>
    <%= link_to '編集', edit_player_path(@player), class: 'btn btn-outline-dark' %>
  </div>
</div>

<% if flash[:notice] %>
  <div class="alert alert-primary"><%= flash[:notice] %></div>
<% end %>

<%= render @player %>
